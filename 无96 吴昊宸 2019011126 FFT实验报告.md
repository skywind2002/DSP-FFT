# 《数字信号处理》课程设计 FFT的编写与测试

>**无96 吴昊宸 2019011126** 

## 一、实验目标

- 编写任意2的整数次幂点数的基2 FFT的通用C/C++程序

- 与直接计算DFT进行比较，分析运行时间上的差异

- 利用所编写的FFT程序完成对如下连续信号频谱的分析
  $$
  s(t)=0.8\times\sin(2\pi\times103t)+\sin(2\pi\times107t)+0.1\times\sin(2\pi\times115t)
  $$
  

## 二、实验原理与说明

​    在这个部分，我将阐述我代码中三个头文件：DFT.h(实现暴力朴素的DFT),2BaseDIT-FFT.h(实现课上所说的DIT-FFT),2BaseDIF-FFT(实现课上所说的DIF-FFT)的设计思路与实现方式。

1. **暴力朴素DFT（头文件DFT.h）**

   实现暴力而朴素的DFT，只需要按DFT的公式进行循环计算即可：

   <img src="C:\Users\23358\AppData\Roaming\Typora\typora-user-images\image-20211026200559567.png" alt="image-20211026200559567" style="zoom:50%;" />

   只需要对式子中的n和k进行一个两重循环就可以了。具体核心代码见下：

   ```C++
   for (k = 0; k < len; k++)
   {
     complex sum; //预备DFT/IDFT右边式子的求和
     for (n = 0; n < len; n++)
     {
       sum += x[n] * (sign == -1 ? complex(cos(2 * PI * n * k / len), -sin(2 * PI * n * k / len)) : complex(cos(2 * PI * n * k / len), sin(2 * PI * n * k / len)));
     }
     X[k] = sum;
   }
   ```

   这样就将x[n]的数据进行了离散时间傅里叶变换，成为X[k]。不仅如此，我的程序还添加了IDFT的选项，当sign=-1的时候，程序将执行DFT，而当sign=+1的时候，程序会执行IDFT

2. **按时间抽取基2FFT（头文件2BaseDIT-FFT.h）**

   根据我的搜索与调查，DIT-FFT和DIF-FFT都有着迭代的简易写法。但是我为了深化自己对于FFT理论上的理解，我选择了遵循课件中的图进行计算。对于DIT-FFT的程序编写，我选择了输入序列为正序，而输出序列为倒位序的蝶形图(如下)：

   <img src="C:\Users\23358\AppData\Roaming\Typora\typora-user-images\image-20211026201111959.png" alt="image-20211026201111959" style="zoom:50%;" />

   在这种情况下，DIT-FFT的计算需要有着三重循环，最外层的k循环控制计算的级数(一共有$r=log_2n$级计算)，中间层的j循环指向每组蝶形(第r级有着$1<<(r-k)$组蝶形，比如图中第一级有着1组一共4个蝶形，第二级有着2组，每组2个蝶形)，而最内层的i循环指向特定组中的每个蝶形进行运算(在第r级，每组蝶形有着$1<<(r-k-1)$个蝶形)。

   对于每个蝶形中的运算，需要让序列号靠后的那个数先乘以旋转因子的幂次，然后两者相加/减。**在这里旋转因子的幂次就是循环变量j的(r-1)位二进制逆序所对应的十进制数**。

   这样的三重循环下，就可以得到输入序列x[n]的DFT X[k]的倒位序排列。在对X[k]重新排列就可以得到输出了。

   **同时需要提到的是，在这个程序中，sign对于进行FFT还是IFFT的运算是通过控制旋转因子上的幂次正负来完成的。**

   程序核心代码如下：

   ```c++
   for (i = 0; i < len / 2; i++)
     {
       W[i] = complex(cos(sign * i * PI * 2 / len), sin(sign * i * PI * 2 / len)); //计算旋转因子
     }
   
     for (k = 0; k < r; k++) //进行r级计算
     {
       for (j = 0; j < 1 << k; j++) //每一级有(1<<k)组蝶形运算
       {
         dist = (1 << (r - k));         //蝶形之间两个点的距离*2，也是相邻组蝶形之间的距离
         for (i = 0; i < dist / 2; i++) //每组中蝶形运算的次数
         {
           shifting = j * dist; //计算蝶形偏移量
           X2[i + shifting] = X1[i + shifting] + X1[i + shifting + dist / 2] * W[reverse(j, r - 1)];
           X2[i + shifting + dist / 2] = X1[i + shifting] - X1[i + shifting + dist / 2] * W[reverse(j, r - 1)]; //乘以旋转因子
         }
       }
       SwapComplex(X1, X2);
     }
   
     for (i = 0; i < len; i++) //倒位序重新排列
     {
       int p = 0;
       for (j = 0; j < r; j++)
       {
         if (i & (1 << j))
           p += 1 << (r - j - 1);
       }
       if (sign == 1)
         Input[i] = X1[p] / complex(len, 0);
       else
         Input[i] = X1[p];
     }
   ```

3. **按频率抽取基2FFT（头文件2BaseDIF-FFT.h）**

   DIF-FFT的代码与DIT-FFT大体一致，我按照PPT上的图进行了编写：

   <img src="C:\Users\23358\AppData\Roaming\Typora\typora-user-images\image-20211026202024297.png" alt="image-20211026202024297" style="zoom:50%;" />

   可以看到，DIF-FFT和DIT-FFT两者的最大区别在于：**DIT是先乘旋转因子再加减，而DIF是先加减再乘以旋转因子**，同时两者的旋转因子公式也有一些区别。在DIF的程序上，我们只需要修改一点点就可以得到DIT的程序：

   ```C++
   for (i = 0; i < len / 2; i++)
     {
       W[i] = complex(cos(sign * i * PI * 2 / len), sin(sign * i * PI * 2 / len)); //计算旋转因子
     }
   
     for (k = 0; k < r; k++) //进行r级计算
     {
       for (j = 0; j < 1 << k; j++) //每一级有(1<<k)组蝶形运算
       {
         dist = (1 << (r - k));         //蝶形之间两个点的距离*2，也是相邻组蝶形之间的距离
         for (i = 0; i < dist / 2; i++) //每组中蝶形运算的次数
         {
           shifting = j * dist; //计算蝶形偏移量
           X2[i + shifting] = X1[i + shifting] + X1[i + shifting + dist / 2];
           X2[i + shifting + dist / 2] = (X1[i + shifting] - X1[i + shifting + dist / 2]) * W[i * (1 << k)]; //乘以旋转因子
         }
       }
       SwapComplex(X1, X2);
     }
   
     for (i = 0; i < len; i++) //倒位序重新排列
     {
       int p = 0;
       for (j = 0; j < r; j++)
       {
         if (i & (1 << j))
           p += 1 << (r - j - 1);
       }
       result[i] = Input[p];
     }
   
     for (i = 0; i < len; i++) //倒位序重新排列
     {
       int p = 0;
       for (j = 0; j < r; j++)
       {
         if (i & (1 << j))
           p += 1 << (r - j - 1);
       }
       if (sign == 1)
         Input[i] = X1[p] / complex(len, 0);
       else
         Input[i] = X1[p];
     }
   ```

   可以看到，两个程序的差距几乎只有在三重循环的最内层的计算公式差异而已。

   至此，我的DFT和FFT的设计思路就阐释清楚了。

**注：程序代码中complex是我自行定义的复数运算类，具体见头文件complex.h**



## 三、运行时间比较

利用ctime库中的记录时间clock()，我们可以测出程序运行所需要的精确时间，只需要在函数执行前后分别取一次clock()然后相减就可以了。

数据方面，我利用C++中的random库中的随机数引擎生成了(-5,5)之间的随机数作为数据的实部和虚部。然后从$2^1$到$2^{14}$进行DFT点数上的循环(点数再大运行时间就很长了)。对于每次循环，都生成相应的随机数列进行DFT，DIT-FFT和DIF-FFT，用clock分别计时存在数组中。我还检验了三个算法所输出序列的一致性，结果当然是一致的。

然后借助于MATLAB的帮助，我画出了DFT点数-时间的图表如下：

<img src="C:\Users\23358\AppData\Roaming\Typora\typora-user-images\image-20211026203308486.png" alt="image-20211026203308486" style="zoom:50%;" />

在这张图中，粉色的是DFT的用时，而DIT-FFT(绿线)和DIF-FFT(蓝线)几乎重合，因此只能看见蓝线。

可以看到，暴力DFT所需要的时间在点数增大的时候快速上涨。当变换点数为$2^{14}$的时候，朴素DFT就需要运行半分钟的，而此时DIT-FFT和DIF-FFT只需要不到1s。

因此可以得到结论：**FFT的时序性能优于直接计算DFT的性能，这个差距在变换点数越大的时候越明显。而在FFT算法内部，不使用迭代算法的情况下，DIT-FFT和DIF-FFT两种算法所需要的时间并没有很大区别。**



## 四、频谱分析的实际操作



